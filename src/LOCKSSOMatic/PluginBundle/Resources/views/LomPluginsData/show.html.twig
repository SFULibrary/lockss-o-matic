{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LomPluginsData</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Domain</th>
                <td>{{ entity.domain }}</td>
            </tr>
            <tr>
                <th>Objectid</th>
                <td>{{ entity.objectId }}</td>
            </tr>
            <tr>
                <th>Key</th>
                <td>{{ entity.key }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ entity.value }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('lompluginsdata') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('lompluginsdata_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
