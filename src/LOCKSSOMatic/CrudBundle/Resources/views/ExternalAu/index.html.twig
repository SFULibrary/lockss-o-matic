{% extends '::base.html.twig' %}


{% block pageheader %}
    <h1>ExternalAu List</h1>
{% endblock %}

{% block body -%}

    <div class="navigation">
        {{ knp_pagination_render(entities) }}
    </div>

    <table class="records_list table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Plnid</th>
                <th>Title</th>
                <th>Auid</th>
                <th>Path</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('externalau_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.plnId }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.auid }}</td>
                <td>{{ entity.path }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('externalau_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('externalau_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(entities) }}
    </div>

        <ul>
        <li>
            <a href="{{ path('externalau_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
