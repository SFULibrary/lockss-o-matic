{% extends '::base.html.twig' %}


{% block pageheader %}
    <h1>DepositStatus List</h1>
{% endblock %}

{% block body -%}

        <table class="table table-striped table-condensed table-bordered table-hover">
        <thead>
            <tr>
                <th>Deposit</th>
                <th>Agreement</th>
                <th>Querydate</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('depositstatus_show', { 'id': entity.id, 'depositId': depositId }) }}">{{ entity.deposit.uuid }}</a></td>
                <td>{{ entity.agreement }}</td>
                <td>{% if entity.queryDate %}{{ entity.queryDate|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(entities) }}
    </div>

{% endblock %}
