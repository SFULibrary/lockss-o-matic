{% extends '::base.html.twig' %}


{% block pageheader %}
    <h1>ContentProvider List</h1>
{% endblock %}

{% block body -%}

	<div class="btn-toolbar pull-right">
        <div class="btn-group">
            <a href="{{ path('contentprovider_new') }}" class="btn btn-default"><span class="glyphicon glyphicon-plus"></span> New</a>
        </div>
    </div>
	<table class="table table-striped table-condensed table-bordered table-hover">
        <thead>
            <tr>
                <th>Uuid</th>
                <th>Permissionurl</th>
                <th>Name</th>
                <th>Maxfilesize</th>
                <th>Maxausize</th>
            </tr>
        </thead>
        <tbody>
			{% for entity in entities %}
				<tr>
					<td><a href="{{ path('contentprovider_show', { 'id': entity.id }) }}">{{ entity.uuid }}</a></td>
					<td>{{ entity.permissionurl }}</td>
					<td>{{ entity.name }}</td>
					<td>{{ entity.maxFileSize }}</td>
					<td>{{ entity.maxAuSize }}</td>
				</tr>
			{% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(entities) }}
    </div>

{% endblock %}
