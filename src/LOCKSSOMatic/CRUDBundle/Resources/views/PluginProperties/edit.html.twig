{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PluginProperties edit</h1>

    {{ form(edit_form, { 'style': 'horizontal' }) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('pluginproperties') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
